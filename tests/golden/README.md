# Golden configs

3 reference wings + `expected/*.json` (with provenance notes) populated
starting P2 (§11 — regression tripwire; `make regress` runs all completed
gates against these). Empty during P0/P1.
