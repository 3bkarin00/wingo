{
  "name": "wingstructgen-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "P10 Web UI (plan.md §9 P10) — React + three.js viewer against the real FastAPI backend (D1). Populated starting P10; empty placeholder during P0-P9.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc -b && vite build",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/three": "^0.165.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
