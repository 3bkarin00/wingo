# R0 Findings — P0

## probe_ocp.py
- cadquery version: `2.8.0`
- `cq.Workplane('XY').box(10, 20, 30).val().Volume()` = 6000.0 (expected 6000, delta 0)
- `BRepCheck_Analyzer(solid.wrapped).IsValid()` = True
- entry point confirmed: `cadquery.Workplane`, `.val().wrapped` gives the underlying `OCP.TopoDS.TopoDS_Shape` for direct OCP calls.

## probe_gmsh.py
- gmsh version: `4.15.2`
- unit-square mesh: 5 nodes, 4 2D elements (surface tag 1)
- entry points confirmed: `gmsh.model.geo.add*` for CAD-lite construction, `gmsh.model.mesh.generate(2)`, `gmsh.model.mesh.getNodes/getElements` for extraction.

