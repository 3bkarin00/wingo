# Known Issues — superseded by docs/kb/

This file's content (OCC/Gmsh/ezdxf workaround knowledge base) migrated to
`docs/kb/` on 2026-07-19 — see `docs/kb/INDEX.md`, filter by `tag:incident`
for exactly the entries that used to live here, or `make kb-search
Q="<term>"` for anything else.

Kept as a pointer file (not deleted) so old links/references still land
somewhere useful. Do not add new entries here — add a `docs/kb/*.md` entry
instead and run `make kb-index`.
